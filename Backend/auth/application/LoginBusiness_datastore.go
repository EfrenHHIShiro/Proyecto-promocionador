package application

import (
	"context"
	"promocionadorApi/auth/domain"
)

func (r *authRepository) LoginBusiness(ctx context.Context, objeto *domain.LoginBusiness) (*domain.AuthenticateBusiness, error) {

	return nil, nil
}
